{
  "profileId": "defaults",
  "description": "Default standalone runtime decisions.",
  "cache": {
    "provider": "vercel-kv",
    "module": "@vercel/kv",
    "notes": "Use platform cache facade; host app provides runtime env + credentials."
  },
  "telemetry": {
    "keyNamespaceMode": "provider-tagged-compat",
    "notes": "Current compatibility mode keeps OpenAI-prefixed token keys."
  },
  "services": {
    "settingsAdapter": "host-runtime",
    "subscriptionAdapter": "host-runtime"
  }
}
